// Phase 4 stub — tiled rendering for GPUs that can't hold A3@300DPI in one pass.
export const TiledRenderer = {
  async render(renderer, scene, camera, width, height) {
    throw new Error('TiledRenderer not implemented until Phase 4.');
  },
};
