// Style module — Phase 2.5. v0.1 ships the pointillism transform.
// User-triggered (not real-time). See docs/modules/style.md and the
// "Data → Style binding" section of DATA-CONTRACTS.md.
export { applyPointillism } from './Pointillism.js';

export const StyleModule = {
  status: 'v0.1',
  styles: ['pointillism'],
};
