# Cycle session note — 2026-04-29 10:12

**Cycle 13 of the /loop 25m orchestration (cron-fired).** Picked queue item (b) from cycle-12: run the v1.4 settings in palette-comparison mode (one source × all palettes) so the v1.4-curation gallery's previously-borrowed v1.3 Marc cell can be replaced with a true-v1.4 Marc output, achieving 100% algorithmic cohesion. Ran `v1.5-palette-comp` with `compare --curated --no-median --width-mm=1.2 --brush-stroke=2.0 --density=0.03` on the alpine-sunset source against all 7 curated palettes — 24.5 s avg, all PASS. Spot-checked the Marc output: the v1.4 settings preserve Marc's primary-tension carnival but with more controlled / more structurally-readable strokes than v1.3's heavier 2.0 mm version. Updated `scripts/build-gallery.js` to point the v1.4-curation gallery's Marc cell at `2026-04-29-pointillism-v1.5-palette-comp/alpine-sunset__marc-symbolic-pointillism.png` and rebuilt the composite. **Result:** the v1.4-curation gallery is now 100% v1.4 settings — texture density and stroke width consistent across all six cells, so the painter-to-painter differences read purely as palette choice rather than algorithm tuning. **Cycle 13 deliverable:** v1.5-palette-comp directory with 7 alpine-source × all-palettes outputs (which also makes future galleries trivial to assemble without re-running the algorithm), plus the regenerated `best-of-6.png` in the v1.4-curation gallery directory. Next-cycle hints: (a) curate 2 more painter palettes (Klimt golden, Macke Tunisian) to widen the emotional register — palettes.json grows to 9; once added, run another v1.4-palette-comp to render them, then a 3×3 gallery becomes possible; (b) implement manual ImageData ellipse rasterisation perf optim (still queued, not blocking but would help future scaling); (c) consider rendering v1.4 on a NEW source scene type (urban/desert/snow-blizzard/rainforest) — the synthetic test corpus has been the same 5 scenes for several cycles and adding variety helps the museum pitch story; (d) floating-buildings still parked.
