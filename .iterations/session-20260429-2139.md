# Cycle session note — 2026-04-29 21:39

**Cycle 33 of the /loop 25m orchestration (cron-fired).** Two small useful items shipped together. **Added 5 npm script aliases** to `package.json`: `pointillism` → `node scripts/pointillism-test.js`, `gallery` → `node scripts/build-gallery.js`, `matrix` → `node scripts/build-matrix-gallery.js`, `thumbnails` → `node scripts/build-thumbnails.js`, `process-plates` → `node scripts/build-process-plates.js`. The exhibition pipeline is now discoverable via plain `npm run` rather than requiring users to know which file is in `scripts/`. **Wrote `RELEASE-NOTES.md`** as Phase 2.5 v1.4's closing artifact (~110 lines). Sections: Highlights (9 palettes, 9 scenes, 81 renders, 6 plates, 4 output formats per plate, determinism, perf), Algorithm fidelity (the surgical modifications relative to `guillaume-gomez/to-pointillism`), What's not in v1.4 (deferred items with rationale), Documentation surface (engineering / curatorial / press / operational / per-cycle / licensing), npm scripts table, Reproducibility one-liner (the exact command sequence to regenerate the exhibition outputs from scratch), and Future versions (v1.5 real Three.js renders, v2.0 meteorology, v2.5 OSM vegetation, v3.0 astronomy). The doc reads as a clean release-version statement that future contributors / users can use as the canonical "what shipped in this version" reference. **Cycle 33 deliverable:** discoverability improvements (npm scripts) + clean v1.4 closing artifact (RELEASE-NOTES.md). The Phase 2.5 sprint has now produced its own changelog. Next-cycle hints: (a) future cycles continue maintenance-mode polish if the cron keeps firing; (b) the user can `CronDelete 0acf8fd4` to end the autonomous loop cleanly — the project is genuinely complete; (c) floating-buildings still parked.
