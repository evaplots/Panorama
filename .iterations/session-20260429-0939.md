# Cycle session note — 2026-04-29 09:39

**Cycle 12 of the /loop 25m orchestration (cron-fired).** Picked the tighter v1.4-leaning best-of gallery from PROJECT-STATE.md queue item 2. The original `best-of-mixed` gallery from cycle 9 was useful but mixed three different iteration vintages (v0.8, v1.0, v1.3); now that v1.4 has been validated as the sweet-spot recommended preset, a cohesive v1.4 showcase is a stronger museum pitch. **Refactored `scripts/build-gallery.js`** to support named galleries via CLI arg: `node scripts/build-gallery.js best-of-mixed` (existing) or `node scripts/build-gallery.js v1.4-curation` (new), with the headline text and pick list controlled by the GALLERIES record. Built the v1.4-curation gallery with 5 cells from `v1.4-mid-stroke` (storm-seascape × Nolde, coastal-twilight × Munch, mountain-twilight × Whistler, forest-noon × Soutine, alpine-sunset × Kirchner) plus 1 carryover from `v1.3-expressionist-comp` (alpine-sunset × Marc — v1.4 doesn't have a Marc run yet, so the v1.3 cell stands in as a final flourish demonstrating the heavier-stroke variant). Output: `.iterations/2026-04-29-best-of-v1.4-curation/best-of-6.png` at A3 @ 300 DPI. Visually the new gallery is more cohesive than the mixed version because the five v1.4 cells share identical settings (1.2 mm stroke width, brush-stroke 2.0, density 0.03, no-median, curated palette) — each painter's character translates through the same algorithmic substrate, making the differences read as palette/emotion rather than algorithm tuning. **Cycle 12 deliverable:** generalized gallery script + the new v1.4 hero image. Both galleries coexist; the user can compare or pick whichever fits a pitch context. Next-cycle hints: (a) implement the manual ImageData ellipse rasterisation perf optim (top-of-queue, deferred from cycle 11 when mid-stroke shipped first) — would speed up future v1.4 batches and unlock denser pointillism modes; (b) run v1.4 in palette-comparison mode (`compare` flag) on alpine source × all 7 palettes to fill the missing Marc cell with a true v1.4 output, then rebuild gallery for a 100%-v1.4 composite; (c) curate 2 more painter palettes (Klimt golden, Macke Tunisian) — palettes.json grows to 9 painters; (d) floating-buildings still parked.
