# Cycle session note — 2026-04-29 15:09

**Cycle 23 of the /loop 25m orchestration (cron-fired).** Picked queue item (c) from cycle-22: process plates showing source-and-painted side-by-side for each EXHIBITION.md plate. Useful for talks / making-of / pitches where the algorithm-in-action is the point rather than the final painting alone. **Wrote `scripts/build-process-plates.js`** — for each of the six plates: load source PNG and painted PNG from the matching iteration directory, compose into an A3 portrait canvas (3508×4961), header strip at top with project + plate title, source cell with "Source — synthetic landscape" small-caps label, painted cell with "Painted — pointillism v1.4 (curated palette, 1.2 mm strokes)" small-caps label, and a wrapped-text caption at the bottom that excerpts the plate's wall-label sentence from EXHIBITION.md. **Geometry math:** each cell preserves the original 1.41:1 landscape aspect, scaled to fit the A3-portrait width (cellW = ~3448, cellH = ~2438), with header (130 px), two label strips (70 px each), and caption (200 px) absorbing the remaining vertical space plus 5×30 px of padding gaps. Six process plates rendered cleanly to `exhibition/process/` (one per plate). Updated `exhibition/README.md` to document the new sub-directory: file table, regeneration command. **Cycle 23 deliverable:** six A3-portrait process plates ready for embedded use in slide decks or talk handouts; the build script is reusable for any future plate updates. **Total exhibition package after this cycle:** EXHIBITION.md (curator-facing wall labels), exhibition/ (six print-ready A3 PNGs), exhibition/process/ (six A3-portrait process plates), exhibition/README.md (mapping + print recommendations). The user can now hand a complete museum-submission package — print files, wall labels, AND a process explainer for the curator — without further editing or assembly. Next-cycle hints: (a) low-res JPEG thumbnails of the six plates for digital pitches/email/web embedding (the originals are 12-14 MB; a 1500-px-wide JPEG version would be ~200 KB); (b) full 9×9 = 81-cell matrix render still queued (would take ~30 min, single dedicated cycle that intentionally overruns); (c) consider an "all six plates" contact-sheet image (3×2 of just the six EXHIBITION plates, distinct from the existing best-of-* galleries which mix-and-match across iterations); (d) floating-buildings still parked.
