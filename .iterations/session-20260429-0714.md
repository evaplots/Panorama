# Cycle session note — 2026-04-29 07:14

**Cycle 6 of the /loop 25m orchestration (cron-fired) — long cycle, two distinct phases.**

**Phase A: impasto experiment win.** Started by picking the impasto hypothesis from the v0.9-dense session note: density 0.025 + brushStrokeFactor 3.5 + thickness 11 px. Added `--brush-stroke=N`, `--width-mm=N`, `--dpi=N`, `--opacity=N`, `--temperature=N` CLI flag overrides to the test harness so future tuning experiments don't need code edits. Ran v1.0-impasto on the 2 strongest scenes (storm-seascape, mountain-twilight) — visually richer than v0.8 AND faster (12.6s vs 18.1s; fewer strokes outweighs larger ellipse cost). Confirmed on all 5 scenes with v1.0-impasto-all (15.6s avg, all PASS, all visually stronger than v0.8). Promoted impasto config to the new defaults in `src/style/Pointillism.js`.

**Phase B: user redirect to algorithm-fidelity.** User requested the pointillism conversion default must follow `guillaume-gomez/to-pointillism` EXACTLY: ColorThief-equivalent palette extraction from the rendered scene, saturation+hue-rotation palette extension, Gaussian gradient smoothing at radius=max(w,h)/50, 11×11 median blur underpainting, weighted-random palette sampling. Plus brushWidthMm physical-measurement parameter (default 0.7 mm at 300 DPI = ~8.27 px) replacing the pixel-based brushThickness. Curated painter palettes become opt-in. **Built v1.1-faithful**: created `src/style/algorithm.js` with `extractPalette` (median-cut), `extendPalette` (sat+hue rotation), `smoothGradient` (3-pass box blur ≈ Gaussian), `medianBlur11` (sliding-histogram, edge-clamped, RGB-per-channel). Refactored `src/style/Pointillism.js`: new DEFAULTS (brushWidthMm=0.7, dpi=300, palette=null=extract, applyMedianUnderpaint=true, smoothGradientField=true, extendPalette=true). Pipeline now: read → palette extract+extend → median underpaint → grayscale → Scharr → smooth gradient → stroke pass with weighted-random palette sampling. Test script gains `--curated`, `--no-median`, `--no-smooth`, `--no-extend` flags for ablations and opt-in to curated mode.

**Verdict:** v1.1-faithful runs at 43.9 s avg on 2 scenes (BORDERLINE) — the median blur is the dominant cost at ~17 s/scene, gradient smoothing adds ~2 s. Visual character is classical pointillism: smaller dots, smoother colour transitions, palette derived from the actual scene. Different mood from v1.0-impasto (which was expressionist Soutine-style) — exactly what the user requested. The `--curated --no-median` opt-in path was sanity-checked on storm-seascape × Nolde and produces the bold expressionist mode for users who want it.

**DATA-CONTRACTS.md updated** with three new sub-sections under "Data → Style binding": stroke-width-as-physical-measurement (brushWidthMm + dpi), palette extraction-vs-curated semantics (palette / paletteSize / extendPalette / sat-boost / hue-jitter / temperature), and underpainting + gradient smoothing options. Default values and bounds documented.

**Next-cycle hints:** (a) optimise the 11×11 median blur — current sliding-histogram implementation iterates 256 bins per slide; a per-bin-delta approach would drop runtime ~5×, putting v1.1-faithful comfortably back into PASS territory; (b) verify v1.1-faithful in browser via `Test pointillism` button (the dev server is currently running for visual QA); (c) run v1.1-faithful on all 5 scenes for a complete benchmark (only 2 done this cycle); (d) circle back to floating-buildings if user provides location.
